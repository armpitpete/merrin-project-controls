# Examples

`project-status-pass.json` demonstrates a truthful `merged` claim. Its planning estimate is 100%, but deployment, live behaviour and human acceptance are explicitly not applicable to that bounded claim. The example does not prove any real consumer or deployment.
